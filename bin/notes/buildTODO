#!/bin/sh
todoFile="/home/pawel/o/notes/todos.md"

target="$(dirname "$todoFile")/pdf"
outputFile="$(basename "$todoFile" .md).pdf"

mkdir -p $target

pandoc \
				--pdf-engine=xelatex \
				-o "$target/$outputFile" $todoFile &

notify-send "TODOs has been built"

#!/bin/sh

filename=$1
target="$(dirname "$filename")/pdf"
outputFile="$(basename "$filename" .md).pdf"

mkdir -p $target

notify-send "Building notes"

pandoc \
				--pdf-engine=xelatex \
				-V "mainfont:DejaVu Sans" \
				-V "geometry:margin=0in" \
				-o "$target/$outputFile" $filename &
